
[Unit]
Description=Application Daemon for {{ NAME }}
After=network.target

[Service]
WorkingDirectory={{ROOT}}
ExecStart={{ctl}} run
{% if systemd.KillSignal %}
KillSignal={{systemd.KillSignal}}
{% endif %}
User={{UID}}
Group={{GID}}

[Install]
WantedBy=multi-user.target
